# Database backups

Full SQL dumps with production data and credentials were removed from this repository for security.

Use the sanitized schema instead:

```
database/schema.sql
```

For local backups, store `.sql` files outside the git repository or in a secure location.
